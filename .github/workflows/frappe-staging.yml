name: Frappe CI/CD - Staging

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository (trigger only)
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Deploy to Frappe Server
        run: |
          ssh ubuntu@${{ secrets.SERVER_IP }} << 'EOF'
            set -e

            sudo -u frappe bash -lc '
              echo "➡️ Moving to bench directory"
              cd /home/frappe/frappe-bench

              echo "➡️ Updating app code (staging branch)"
              cd apps/${{ secrets.FRAPPE_APP }}
              git fetch origin
              git checkout staging
              git pull origin staging

              echo "➡️ Running database migrations"
              cd /home/frappe/frappe-bench
              bench migrate

              echo "➡️ Restarting Frappe services"
              bench restart

              echo "✅ Staging deployment completed successfully"
            '
          EOF
