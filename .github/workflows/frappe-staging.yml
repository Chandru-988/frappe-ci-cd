#HEllo
name: Frappe CI/CD - Staging

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository (trigger only)
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Deploy to Frappe Server
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.SERVER_IP }} << 'EOF'
            set -e

            sudo -u frappe bash -lc '
              echo "➡️ Entering bench"
              cd /home/frappe/frappe-bench

              echo "➡️ Updating app (staging branch)"
              cd apps/${{ secrets.FRAPPE_APP }}
              git fetch origin
              git checkout staging
              git pull origin staging

              echo "➡️ Migrating"
              cd /home/frappe/frappe-bench
              bench migrate

              echo "➡️ Restarting"
              bench restart

              echo "✅ CI/CD deploy finished"
            '
          EOF
